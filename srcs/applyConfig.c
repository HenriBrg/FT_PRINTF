/*
** La fonction applyConfig() gère la précision, la width puis les flags(1 & 2 & 3)
*/

void applyConfig(t_printf *tab)
{
  /* Ordre d'application de la config:
  ** 1/ Type (via les appels de fonctions dans handleDisplay)
  ** 2/ Size / length (via les appels de fonctions dans handleDisplay)

  ** 3/ Précision appl
  ** 4/ Width
  ** 5/ Flags
  */

  (void)tab;
}
